{
  "name": "trpc",
  "version": "1.0.0",
  "scripts": {
    "generate": "yarn workspace app-types build",
    "lint": "eslint .",
    "syntax-check": "tsc --emitDeclarationOnly false --noEmit true",
    "ci": "eslint . && tsc --emitDeclarationOnly false --noEmit true"
  },
  "dependencies": {
    "app-types": "1.0.0",
    "zod": "^3.23.8"
  },
  "peerDependencies": {
    "@trpc/client": "*",
    "@trpc/server": "*",
    "@typescript-eslint/eslint-plugin": "*",
    "@typescript-eslint/parser": "*",
    "typescript": "*"
  }
}
