datasource db {
  provider = "mysql"
  url = env("DATABASE_URL") 
}

// Third party authentication
model User {
  provider    String
  id          String @id
  displayName String
  email       String?
}
